{
  "name": "platform-home-assignment",
  "version": "1.0.0",
  "description": "Mock DSPM Portal for automation testing",
  "scripts": {
  "test": "npx playwright test",
  "test:ui": "npx playwright test tests/ui",
  "test:api": "npx playwright test tests/api",
  "test:headed": "npx playwright test tests/ui --headed",
  "test:debug": "npx playwright test tests/ui --debug",
  "test:pw-ui": "npx playwright test --ui",
  "test:report": "npx playwright show-report"
},
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
